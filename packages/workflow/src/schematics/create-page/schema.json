{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsCreatePage",
  "title": "Creates a new raw averos page and register a menu entry to that page in the application",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the page.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of your page ?"
    },
    "updateRouteMenu": {
      "description": "If route & menu should be updated.",
      "type": "boolean",
      "default": false,
      "x-prompt": "Do you want to add a default route and Menu link?"
    },
    "targetMenu": {
      "description": "The target place into which the menu entries will be placed",
      "type": "string",
      "enum": [
                "side",
                "top",
                "both"
              ]
    },
    "space": {
      "description": "The target space into which the menu entries will be available",
      "type": "string",
      "enum": [
                "logged",
                "public"
              ]
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the use case component.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
       }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "name"
  ]
}
