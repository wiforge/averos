{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsAddSimpleMemberEnumeration",
  "title": "Generates a new Simple member of type enumeration for the Entity ename",
  "type": "object",
  "properties": {
    "ename": {
      "description": "The name of the entity.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the Entity to which you would like to add your new member?"
    },
    "mname": {
      "description": "The simple member name (the field name).",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the simple member that you wish to add to your entity?"
    },
    "listOfEnumValues": {
      "description": "The list of the enumeration values seperated by ','",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the list of your enumeration values (values are separated by ',': ex: val1,val2,val3) ?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to source file.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
       }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "listOfEnumValues"
  ]
}
