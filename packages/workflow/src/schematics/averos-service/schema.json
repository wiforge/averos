{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsAverosService",
  "title": "Generates a service named name for the entity ename",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the service.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the entity service you would like to create?"
    },
    "ename": {
      "description": "The target entity name.",
      "type": "string",
      "x-prompt": "What is the name of the target entity?"
    },
    "assignToEntity": {
      "description": "Links and assign the service to the entity",
      "type": "boolean",
      "default": false,
      "x-prompt": "Would you like to link your entity to your service (updates the entity) (NO)?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
       }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "name"
  ]
}
