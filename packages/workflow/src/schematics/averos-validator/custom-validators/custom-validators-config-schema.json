{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "SchematicsCustomValidator",
  "title": "Generates a validator for a specific entity field",
  "type": "object",
  "properties": {
    "name": {
      "description": "The name of the validator.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the validator you would like to create?"
    },
    "entityName": {
      "description": "The target entity name.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the target entity?"
    },
    "fieldName": {
      "description": "The target entity field name.",
      "type": "string",
      "minLength": 1,
      "x-prompt": "What is the name of the entity field , subject to the validation ?"
    },
    "nature": {
      "description": "The Validator nature: sync/async",
      "type": "string",
      "minLength": 1,
      "enum": [
        "sync",
        "async"
      ],
      "x-prompt": {
        "message": "What is the nature of the validator (sync/async) ? [sync]",
        "type": "list",
        "items": [
          { "value": "sync", "label": "Synchronous" },
          { "value": "async", "label": "Asynchronous" }
        ]
      }
    },
    "validatorId": {
      "description": "The validation method name.",
      "type": "string",
      "x-prompt": "What is the name of your validation method ?"
    },
    "validatorKey": {
      "description": "The validation key.",
      "type": "string",
      "x-prompt": "What is the name of your validation key ?"
    },
    "validationParameters": {
      "description": "The validation parameters used with the validation method (comma-separated values).",
      "type": "string",
      "default": "none",
      "x-prompt": "What are the parameters values needed for your validation action (comma-separated values) [none]?"
    },
    "validationDefaultMessage": {
      "description": "The default validation message.",
      "type": "string",
      "default": "A validation error occured!",
      "x-prompt": "What is the default message you want to display for your validation ?"
    },
    "validationMessageTranslationId": {
      "description": "The translation id related to the validation message.",
      "type": "string",
      "x-prompt": "Please define a translation identifier for your message if you want to include it in the multi-language support ?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service.",
      "visible": false,
      "$default": {
        "$source": "workingDirectory"
       }
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
   },
  "required": [
    "name",
    "entityName",
    "fieldName"
  ]
}
